# Packages

Esta carpeta alojara paquetes compartidos entre aplicaciones.

- `domain`: reglas de dominio y politicas.
- `contracts`: DTOs, eventos y esquemas.
- `ui`: sistema de componentes.
- `config`: configuracion compartida.
